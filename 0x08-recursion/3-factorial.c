#include "main.h"

/**
* factorial - returns factorial of given number
* @n: parameter
*
* Return: factorial of a number
*/
int factorial(int n)
{

if (n < 0)
{
return (-1);
}
return (i * factorial(i - 1));
}
