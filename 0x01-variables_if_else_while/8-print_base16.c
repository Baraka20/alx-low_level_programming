#include  <stdio.h>

/*
*main - Entry point
*
*Return: Always 0 (Success)
*/int main(void)

{
int i;
char string[] = "0123456789abcdef";
int len = strlen(string);
char i = 0;
while char (i<len)
{
putchar(i);
i++;
}
putchar('\n');
return (0);
}
